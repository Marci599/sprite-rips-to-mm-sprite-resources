# Copilot Instructions

## Project Guidelines
- User changed the config folder name in code to remove 'MM' — prefers 'FramesToSpriteResources' instead of 'FramesToMMSpriteResources' for the LocalAppData folder.